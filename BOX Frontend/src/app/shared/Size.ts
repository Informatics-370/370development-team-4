export interface Size {
    sizeID: number;
    width: number;
    height: number;
    length: number;
    volume: number;
    weight: number;
    description: string; //later change to category ID when Kuziwa merges his code
};
